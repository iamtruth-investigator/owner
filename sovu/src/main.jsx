import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{createClient}from'@supabase/supabase-js';import'./style.css';
const supabase=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_ANON_KEY);const cats=['Home help','Cleaning','Driving','Cooking','Caregiving','Beauty','Repairs','Moving','Tech','Design & Editing','Tutoring','Car care','Delivery','Events','Other'];
function App(){const[tab,setTab]=useState('home'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[mode,setMode]=useState('login'),[user,setUser]=useState(null),[message,setMessage]=useState(''),[job,setJob]=useState('');useEffect(()=>{supabase.auth.getSession().then(({data})=>setUser(data.session?.user??null));const{data}=supabase.auth.onAuthStateChange((_e,s)=>setUser(s?.user??null));return()=>data.subscription.unsubscribe()},[]);async function auth(e){e.preventDefault();setMessage('');const r=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin}});if(r.error)setMessage(r.error.message);else{setMessage(mode==='login'?'Signed in successfully.':'Check your email to confirm your account.');if(r.data.user)setUser(r.data.user)}}async function signout(){await supabase.auth.signOut();setUser(null)}return <div className="app"><header><b>SOVU</b><span>Get help. Get paid.</span>{user?<button onClick={signout}>Sign out</button>:<button onClick={()=>setTab('join')}>Sign in</button>}</header>{tab==='home'&&<><section className="hero"><p className="pill">ONE PLACE FOR EVERY LAWFUL JOB</p><h1>Need something done?<br/><i>Find someone.</i></h1><p>From a cleaner to a developer, driver, editor, tutor or car washer — post what you need and let trusted people apply.</p><button className="primary" onClick={()=>setTab('post')}>Post a request</button></section><section className="grid">{cats.map(c=><button key={c} onClick={()=>{setJob(c);setTab('post')}}>{c}</button>)}</section><section className="earn"><h2>Turn your skills into income.</h2><p>Create a provider profile, choose your skills, discover nearby work and get paid through secure checkout.</p><button onClick={()=>setTab('join')}>Become a provider →</button></section></>}{tab==='post'&&<section className="panel"><button onClick={()=>setTab('home')}>← Back</button><h2>What do you need?</h2>{user?<><input value={job} onChange={e=>setJob(e.target.value)} placeholder="e.g. Need a cleaner tomorrow"/><textarea placeholder="Describe the work, location and preferred time"/><button className="primary" onClick={()=>setMessage('Request ready to publish — database posting is next.')}>Publish request</button></>:<><p>Please sign in before posting a request.</p><button className="primary" onClick={()=>setTab('join')}>Sign in</button></>}{message&&<div className="success">{message}</div>}</section>}{tab==='join'&&<section className="panel"><button onClick={()=>setTab('home')}>← Back</button>{user?<><h2>You're signed in</h2><p>{user.email}</p><button onClick={signout}>Sign out</button></>:<><h2>{mode==='login'?'Sign in to SOVU':'Create your SOVU account'}</h2><form onSubmit={auth}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" required minLength="6" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="primary" type="submit">{mode==='login'?'Sign in':'Create account'}</button></form><button onClick={()=>{setMode(mode==='login'?'signup':'login');setMessage('')}}>{mode==='login'?'Create an account':'Already have an account? Sign in'}</button>{message&&<div className="success">{message}</div>}</>}</section>}<nav><button onClick={()=>setTab('home')}>Home</button><button onClick={()=>setTab('post')}>Request</button><button onClick={()=>setTab('join')}>Profile</button></nav></div>}createRoot(document.getElementById('root')).render(<App/>);