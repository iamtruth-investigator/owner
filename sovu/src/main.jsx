import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{createClient}from'@supabase/supabase-js';import'./style.css';
const SUPABASE_URL='https://xtlunrelrirjmwrvqlhz.supabase.co';const SUPABASE_PUBLISHABLE_KEY='sb_publishable_RFrH8h6wZbQk7_Cq_zeTRw_IaruRJhF';const supabaseUrl=import.meta.env.VITE_SUPABASE_URL||SUPABASE_URL;const supabaseKey=import.meta.env.VITE_SUPABASE_ANON_KEY||SUPABASE_PUBLISHABLE_KEY;let supabase=null;let supabaseConfigError='';try{supabase=createClient(supabaseUrl,supabaseKey,{auth:{flowType:'pkce',detectSessionInUrl:true}})}catch(e){supabaseConfigError=e instanceof Error?e.message:'Invalid Supabase configuration'}
const serviceCats=['Home help','Cleaning','Driving','Cooking','Caregiving','Beauty','Repairs','Moving','Tech','Design & Editing','Tutoring','Car care','Delivery','Events','Other'];const companionCats=['Social Company','Gaming Companion','Conversation Partner','Senior Companion','Public Event Companion','Travel Companion'];
function App(){const[tab,setTab]=useState('home'),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[mode,setMode]=useState('login'),[user,setUser]=useState(null),[message,setMessage]=useState(''),[job,setJob]=useState(''),[description,setDescription]=useState(''),[location,setLocation]=useState(''),[budget,setBudget]=useState(''),[jobs,setJobs]=useState([]),[loading,setLoading]=useState(false),[profile,setProfile]=useState(null),[editingProfile,setEditingProfile]=useState(false),[profileForm,setProfileForm]=useState({full_name:'',bio:'',city:'',country:'India',role:'Customer',phone:''});useEffect(()=>{if(!supabase)return;supabase.auth.getSession().then(({data})=>{const u=data.session?.user??null;setUser(u);if(u)loadProfile(u)}).catch(e=>setMessage(e.message));const{data}=supabase.auth.onAuthStateChange((_e,s)=>{const u=s?.user??null;setUser(u);if(u)loadProfile(u);else setProfile(null)});return()=>data.subscription.unsubscribe()},[]);useEffect(()=>{if(tab==='jobs')loadJobs()},[tab]);async function loadProfile(u=user){if(!supabase||!u)return;const{data,error}=await supabase.from('profiles').select('id,full_name,avatar_url,bio,city,country,role,phone,verified,created_at').eq('id',u.id).maybeSingle();if(error){setMessage(error.message);return}const p=data||{id:u.id,full_name:u.email?.split('@')[0]||'Helping member',bio:'New to Helping — add a short introduction.',city:'',country:'India',role:'Customer',phone:'',verified:false};setProfile(p);setProfileForm({full_name:p.full_name||u.email?.split('@')[0]||'',bio:p.bio||'',city:p.city||'',country:p.country||'India',role:p.role||'Customer',phone:p.phone||''})}async function saveProfile(){if(!user||!supabase)return;setLoading(true);const{data,error}=await supabase.from('profiles').upsert({id:user.id,full_name:profileForm.full_name.trim()||'Helping member',bio:profileForm.bio.trim(),city:profileForm.city.trim(),country:profileForm.country.trim()||'India',role:profileForm.role,phone:profileForm.phone.trim()},{onConflict:'id'}).select('id,full_name,avatar_url,bio,city,country,role,phone,verified,created_at').single();setLoading(false);if(error){setMessage(error.message);return}setProfile(data);setEditingProfile(false);setMessage('Profile updated successfully.')}async function auth(e){e.preventDefault();if(!supabase)return setMessage('Supabase is not connected.');setMessage('');setLoading(true);const r=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin}});setLoading(false);if(r.error){if(mode==='signup'&&r.error.status===500)setMessage('This email may already have an account. Try Sign in, or use a different email.');else setMessage(r.error.message)}else{setMessage(mode==='login'?'Signed in successfully.':'Account created. Check your email to confirm your account.');if(r.data.user){setUser(r.data.user);await loadProfile(r.data.user)}}}async function signout(){if(supabase)await supabase.auth.signOut();setUser(null);setProfile(null);setTab('home')}async function loadJobs(){if(!supabase){setMessage('Supabase is not connected yet.');return}setLoading(true);const{data,error}=await supabase.from('jobs').select('id,title,description,location,budget,currency,urgency,created_at').eq('status','open').order('created_at',{ascending:false}).limit(30);setJobs(data||[]);if(error)setMessage(error.message);setLoading(false)}async function publish(){if(!user)return setTab('join');if(!supabase)return setMessage('Supabase is not connected yet.');if(!job.trim())return setMessage('Add a title for the request.');setLoading(true);const{error}=await supabase.from('jobs').insert({customer_id:user.id,title:job.trim(),description,location,budget:budget?Number(budget):null});setLoading(false);if(error)setMessage(error.message);else{setMessage('Request published successfully.');setJob('');setDescription('');setLocation('');setBudget('');setTab('jobs')}}const choose=c=>{setJob(c);setTab('post')};const initials=(profile?.full_name||user?.email||'H').split(' ').map(x=>x[0]).join('').slice(0,2).toUpperCase();return <div className="app"><header><b>Helping</b><span>Get help. Get paid.</span>{user?<button onClick={signout}>Sign out</button>:<button onClick={()=>setTab('join')}>Sign in</button>}</header>{supabaseConfigError&&<div className="configNotice">⚙️ <b>Connection issue:</b> {supabaseConfigError}</div>}{tab==='home'&&<><section className="hero"><p className="pill">ONE PLACE FOR EVERY LAWFUL JOB</p><h1>Need something done?<br/><i>Find someone.</i></h1><p>From everyday help to skilled work and safe social companionship — post what you need and let people apply.</p><button className="primary" onClick={()=>setTab('post')}>Post a request</button></section><section className="section"><div className="sectionTitle"><span>Services</span><small>Get things done</small></div><div className="grid">{serviceCats.map(c=><button key={c} onClick={()=>choose(c)}>{c}</button>)}</div></section><section className="companion"><div className="sectionTitle"><span>🤝 Companion</span><small>Lawful social company</small></div><p>Find people for conversation, gaming, public events, travel and non-medical companionship. Identity verification and safety tools are part of the marketplace design.</p><div className="grid">{companionCats.map(c=><button key={c} onClick={()=>choose(c)}>{c}</button>)}</div><div className="safety">🛡️ <b>Safety first:</b> identity checks, reporting and moderation. No sexual services, exploitation or illegal activity.</div></section><section className="earn"><h2>Turn your skills into income.</h2><p>Create a provider profile, discover work and get paid through secure checkout.</p><button onClick={()=>setTab('jobs')}>Browse requests →</button></section></>}{tab==='post'&&<section className="panel"><button onClick={()=>setTab('home')}>← Back</button><h2>Post a request</h2>{job&&<div className="selected">Selected service: <b>{job}</b></div>}{user?<><input value={job} onChange={e=>setJob(e.target.value)} placeholder="What do you need?"/><textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="Describe the work, requirements and preferred time"/><input value={location} onChange={e=>setLocation(e.target.value)} placeholder="Location or Remote"/><input type="number" value={budget} onChange={e=>setBudget(e.target.value)} placeholder="Budget (optional)"/><button className="primary" disabled={loading} onClick={publish}>{loading?'Publishing…':'Publish request'}</button></>:<><p>Please sign in before posting a request.</p><button className="primary" onClick={()=>setTab('join')}>Sign in</button></>}{message&&<div className="success">{message}</div>}</section>}{tab==='jobs'&&<section className="panel"><button onClick={()=>setTab('home')}>← Back</button><h2>Open requests</h2><p className="muted">Final receipts will include approved expenses, Helping fees and applicable taxes once billing is connected.</p>{loading?<p>Loading…</p>:jobs.length?<div className="jobs">{jobs.map(j=><article key={j.id}><h3>{j.title}</h3><p>{j.description||'No description provided.'}</p><small>{j.location||'Location not specified'} {j.budget?`• ${j.currency||'INR'} ${j.budget}`:''}</small><button onClick={()=>setMessage(user?'Application flow is next — this request is live in the marketplace.':'Sign in to apply.')}>{user?'Apply':'Sign in to apply'}</button></article>)}</div>:<p>No open requests yet. Be the first to post one.</p>}{message&&<div className="success">{message}</div>}</section>}{tab==='join'&&<section className="panel">{user?<><div className="profileHero"><div className="avatar">{initials}</div><div><span className="eyebrow">YOUR HELPING PROFILE</span><h2>{profile?.full_name||'Helping member'}</h2><p>{user.email}</p></div><span className="statusDot">● Online</span></div><div className="profileCard"><div className="profileTop"><div><h3>About you</h3><p>{profile?.bio||'Tell people a little about yourself.'}</p></div>{profile?.verified?<span className="verified">✓ Verified</span>:<span className="unverified">○ Not verified</span>}</div><div className="profileDetails"><div><small>Role</small><b>{profile?.role||'Customer'}</b></div><div><small>Location</small><b>{[profile?.city,profile?.country].filter(Boolean).join(', ')||'Add location'}</b></div><div><small>Member since</small><b>{profile?.created_at?new Date(profile.created_at).toLocaleDateString(undefined,{month:'short',year:'numeric'}):'Today'}</b></div><div><small>Phone</small><b>{profile?.phone||'Not added'}</b></div></div><button className="primary compact" onClick={()=>setEditingProfile(!editingProfile)}>{editingProfile?'Cancel editing':'Edit profile'}</button></div>{editingProfile&&<div className="profileCard editCard"><h3>Complete your profile</h3><p className="muted">A fuller profile helps people know who they are working with.</p><input value={profileForm.full_name} onChange={e=>setProfileForm({...profileForm,full_name:e.target.value})} placeholder="Full name"/><textarea value={profileForm.bio} onChange={e=>setProfileForm({...profileForm,bio:e.target.value})} placeholder="Short bio"/><div className="twoCol"><input value={profileForm.city} onChange={e=>setProfileForm({...profileForm,city:e.target.value})} placeholder="City"/><input value={profileForm.country} onChange={e=>setProfileForm({...profileForm,country:e.target.value})} placeholder="Country"/></div><select value={profileForm.role} onChange={e=>setProfileForm({...profileForm,role:e.target.value})}><option>Customer</option><option>Provider</option><option>Both</option></select><input value={profileForm.phone} onChange={e=>setProfileForm({...profileForm,phone:e.target.value})} placeholder="Phone (optional)"/><button className="primary" disabled={loading} onClick={saveProfile}>{loading?'Saving…':'Save profile'}</button></div>}<div className="profileStats"><div><b>0</b><span>Requests posted</span></div><div><b>0</b><span>Jobs completed</span></div><div><b>—</b><span>Rating</span></div></div><div className="profileLinks"><button onClick={()=>setTab('post')}>＋ Post a request <span>→</span></button><button onClick={()=>setTab('jobs')}>⌕ Find work <span>→</span></button><button onClick={()=>setMessage('Verification centre is coming next.')}>✓ Verification <span>→</span></button><button onClick={()=>setMessage('Settings are coming next.')}>⚙ Account settings <span>→</span></button></div>{message&&<div className="success">{message}</div>}</>:<><h2>Sign in to Helping</h2><form onSubmit={auth}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" required minLength="6" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password"/><button className="primary" type="submit" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><button className="authSwitch" onClick={()=>{setMode(mode==='login'?'signup':'login');setMessage('')}}>{mode==='login'?'Create an account':'Already have an account? Sign in'}</button>{message&&<div className="success">{message}</div>}</>}</section>}{tab!=='join'&&null}<nav><button onClick={()=>setTab('home')}>Home</button><button onClick={()=>setTab('post')}>Request</button><button onClick={()=>setTab('jobs')}>Find work</button><button onClick={()=>{if(user)loadProfile(user);setTab('join')}}>Profile</button></nav></div>}createRoot(document.getElementById('root')).render(<App/>);