import React,{useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';
const cats=['Home help','Cleaning','Driving','Cooking','Caregiving','Beauty','Repairs','Moving','Tech','Design & Editing','Tutoring','Car care','Delivery','Events','Other'];
function App(){const[tab,setTab]=useState('home');const[job,setJob]=useState('');const[posted,setPosted]=useState(false);return <div className="app"><header><b>SOVU</b><span>Get help. Get paid.</span><button onClick={()=>setTab('join')}>Join</button></header>{tab==='home'&&<><section className="hero"><p className="pill">ONE PLACE FOR EVERY LAWFUL JOB</p><h1>Need something done?<br/><i>Find someone.</i></h1><p>From a cleaner to a developer, driver, editor, tutor or car washer — post what you need and let trusted people apply.</p><button className="primary" onClick={()=>setTab('post')}>Post a request</button></section><section className="grid">{cats.map(c=><button key={c} onClick={()=>{setJob(c);setTab('post')}}>{c}</button>)}</section><section className="earn"><h2>Turn your skills into income.</h2><p>Create a provider profile, choose your skills, discover nearby work and get paid through secure checkout.</p><button onClick={()=>setTab('join')}>Become a provider →</button></section></>}{tab==='post'&&<section className="panel"><button onClick={()=>setTab('home')}>← Back</button><h2>What do you need?</h2><input value={job} onChange={e=>setJob(e.target.value)} placeholder="e.g. Need a cleaner tomorrow"/><textarea placeholder="Describe the work, location and preferred time"/><button className="primary" onClick={()=>setPosted(true)}>Publish request</button>{posted&&<div className="success">Request published. Matching providers can now apply.</div>}</section>}{tab==='join'&&<section className="panel"><button onClick={()=>setTab('home')}>← Back</button><h2>Join SOVU</h2><div className="roles"><button onClick={()=>setTab('home')}>I need services<br/><small>Customer</small></button><button onClick={()=>setTab('home')}>I want to earn<br/><small>Provider</small></button></div><p className="muted">Verification, payments and identity checks are added before real transactions go live.</p></section>}<nav><button onClick={()=>setTab('home')}>Home</button><button onClick={()=>setTab('post')}>Request</button><button onClick={()=>setTab('join')}>Profile</button></nav></div>}createRoot(document.getElementById('root')).render(<App/>);